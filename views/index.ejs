<%- include('partials/header.ejs') %>
<div class="pb-4">
  <h1>Discover Stories Daily!</h1>
  <hr>
</div>

<div class="d-flex align-items-center justify-content-around col-12 flex-wrap">
<% if(locals.posts) { %>
    <% posts.forEach((post) =>{ %>
  <%-  include("partials/postCards.ejs",{title:post.title,content:post.content,postId:post.id,date:post.date}); %>
   <% }) %>
<%} %>
</div>



<%- include('partials/footer.ejs') %>